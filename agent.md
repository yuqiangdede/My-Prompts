# Agent Guide

本仓库用于管理可检索、可复用、可迭代的提示词集合。

## 目录结构

- 以顶层目录区分场景（如 `01-软件工程/`、`02-产品定义/`）
- 同一场景内按任务类型分组，文件命名采用“编号 + 中文描述”

## 新增或修改提示词

- 先放入最贴切的既有目录；不要随意改动目录结构
- 每个提示词文件保持统一分节：`目标` / `输入` / `约束` / `输出格式` / `示例`
- 文件名延续现有编号与中文风格
- 有新增/移动文件时，更新 `README.md` 的索引与结构说明
- `README.md` 链接需正确处理空格（使用 `%20` 编码）

## 修改原则

- 优先做最小改动，只调整用户明确要求的部分
- 未经要求不要重写已有提示词或改动命名规则
