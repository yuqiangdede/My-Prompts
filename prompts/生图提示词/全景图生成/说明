# 360° 全景图生成与查看器

## 用途

先生成符合等距柱状投影规范的 360° 全景图，再用 Three.js 制作可交互查看器。

## 输入变量

- `SCENE`：场景描述
- `TIME_WEATHER`：时间与天气
- `CAMERA_HEIGHT`：相机高度，默认 1.6 米
- `STYLE`：写实 / 电影感 / 建筑可视化等
- `IMAGE_FILE`：全景图文件名或路径

## Prompt A：生成全景图

```text
生成一张完整的 360° 等距柱状全景图（equirectangular panorama）。

场景：{{SCENE}}
时间与天气：{{TIME_WEATHER}}
风格：{{STYLE|default:高真实性摄影}}
相机高度：约 {{CAMERA_HEIGHT|default:1.6米}}

几何要求：
1. 严格使用 2:1 宽高比，例如 4096×2048 或 8192×4096。
2. 水平视野覆盖完整 360°，垂直视野覆盖从天顶到地面最低点。
3. 地平线保持水平并位于画面垂直中线附近。
4. 左右边缘必须无缝衔接，边缘处不要放置被截断的主要人物、车辆、建筑入口或文字。
5. 避免普通超广角照片、鱼眼圆图、立方体展开图、重复物体和接缝错位。
6. 天顶区域连续自然，不出现放射状拉伸、黑洞或重复天空纹理。
7. 地面最低点避免脚架、摄影者、黑洞、旋涡和严重拉伸。
8. 透视应符合单一固定机位，不要混合多个视角或出现空间结构自相矛盾。
9. 场景细节丰富但分布均衡，前后左右均有可观察内容，不要只在正前方堆叠主体。
10. 不生成文字、Logo、水印、边框或 UI。

负向约束：
非2:1、鱼眼圆图、普通广角、左右接缝、重复人物、重复建筑、断裂道路、弯曲地平线、天空黑洞、地面旋涡、错误透视、文字、水印。
```

## Prompt B：生成 Three.js 查看器

```text
请生成一个可直接运行的单文件 `index.html`，使用 Three.js 将等距柱状全景图渲染成 360° 交互查看器。

全景图：{{IMAGE_FILE}}

功能要求：
1. 使用球体内表面贴图或等效可靠方案，正确映射 equirectangular 图像。
2. 鼠标拖拽旋转视角，滚轮缩放；触摸设备支持单指拖动和双指缩放。
3. 支持全屏、重置视角和本地图片选择。
4. 页面响应式，桌面和手机均可使用。
5. 提供加载状态、图片加载失败提示和 WebGL 不可用提示。
6. 限制俯仰角，避免视角翻转；限制最小和最大 FOV。
7. 动画循环只在视角变化或必要时渲染，避免无意义高占用。
8. 界面简洁，控制按钮固定在页面边缘，不遮挡主要画面。
9. 代码包含必要注释，并说明直接双击打开与本地 HTTP 服务运行的差异。
10. 不依赖后端服务。外部依赖使用明确版本的 Three.js CDN；同时说明如何改成本地文件。

交付内容：
- 完整 `index.html`
- 建议目录结构
- 本地运行命令
- 常见问题：跨域、纹理过大、移动端性能和图像方向错误
```

## 使用说明

全景图的核心不是“电影级”形容词，而是正确的 2:1 投影、无缝接缝和单一机位空间一致性。