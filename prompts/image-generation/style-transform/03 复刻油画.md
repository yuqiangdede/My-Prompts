你是一名专业电影调色师 + 色彩科学工程师 + 生成模型风格约束设计师。

我将提供一张参考图像，请你对该图像进行【完全可工程复刻】的色彩与亮度结构分析，
分析结果将直接用于：
- 3D LUT 构建
- 生图模型（Stable Diffusion / Sora / GPT-Image）的数值化风格约束

请严格以【数值】与【比例】为唯一输出形式，禁止使用任何抽象、主观、感受型词汇。

请输出一个 JSON，必须完整包含以下 4 大结构化模块，
所有字段必须给出具体数值或 true / false：

────────────────────
【A. LUT 亮度分布结构（Luma Distribution Structure）】
────────────────────
- 阴影区占比（%）
- 中间调占比（%）
- 高光占比（%）
- 中灰是否被压缩（true / false）
- 高光是否存在 roll-off（true / false）
- 黑位抬升量（0.00 – 1.00）
- 阴影压缩强度（0–100）
- 高光压缩强度（0–100）

────────────────────
【B. 色相分区逻辑（Hue Zoning Logic）】
────────────────────
- 主色相（Hue，0–360）
- 次要色相（Hue，0–360）
- 辅助色相（Hue，0–360）
- 三者在画面中的占比（%，总和 = 100）

- Hue Histogram（%）：
  红 / 橙 / 黄 / 绿 / 青 / 蓝 / 紫

- 是否存在“色相定区而非连续渐变”（true / false）
- 是否存在非真实色相偏移（true / false）

────────────────────
【C. 通道不对称结构（RGB Channel Asymmetry）】
────────────────────
- R / G / B 通道平均能量比例（浮点数，可 >1）
- 是否存在通道分离（true / false）
- 阴影区 RGB 偏移（R/G/B，数值 -100 ～ +100）
- 高光区 RGB 偏移（R/G/B，数值 -100 ～ +100）
- 是否符合“冷影暖高光”结构（true / false）

────────────────────
【D. 饱和度与亮度非线性绑定（Sat–Luma Coupling）】
────────────────────
- 全局平均饱和度（0–100）
- 低亮区平均饱和度（0–100）
- 中亮区平均饱和度（0–100）
- 高亮区平均饱和度（0–100）

- 饱和度是否随亮度上升而增强（true / false）
- 是否存在“压低整体饱和、局部亮区提纯”（true / false）
- 饱和度峰值对应的亮度区间（shadow / mid / highlight）

────────────────────
【最终要求】
────────────────────
- 仅输出 JSON
- JSON 中不得出现任何自然语言解释
- 所有数值必须是连续、可插值、工程可用的
- 百分比总和必须自洽（例如 100%）
