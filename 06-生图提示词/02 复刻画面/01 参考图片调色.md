你是一名专业电影调色师 + 色彩科学工程师。

我将提供一张参考照片，请你对该照片进行“可工程复刻”的色彩分析，
目标是用于制作 LUT（3D LUT / DaVinci Resolve / Premiere）。

请严格以【数值分析】为主，不要使用抽象描述词。

请输出一个 JSON，包含以下内容（所有字段必须给出具体数值或比例）：

1. 整体色彩结构分析
- 主色相（Hue，0–360°）
- 次要色相（Hue，0–360°）
- 辅助色相（Hue，0–360°）
- 各色相在画面中的占比（百分比，总和 = 100）

2. 色相分布（Hue Histogram）
- 红 / 橙 / 黄 / 绿 / 青 / 蓝 / 紫
- 每个色相区间的像素占比（%）

3. 饱和度分析
- 平均饱和度（0–100）
- 低饱和区 / 中饱和区 / 高饱和区 占比（%）
- 是否存在“压低整体饱和、局部提纯”的策略（true/false）

4. 明度（Luma）结构
- 阴影 / 中间调 / 高光 占比（%）
- 是否存在明显的中灰压缩或高光 roll-off
- 黑位是否抬升（数值 0–1）

5. 色温与色调倾向
- 估算色温（Kelvin）
- 偏暖 / 偏冷（数值 -100 ～ +100）
- 偏绿 / 偏洋红（数值 -100 ～ +100）

6. RGB 通道关系
- R/G/B 通道平均值比例
- 是否存在通道分离（例如蓝影红高光）
- 高光 / 阴影 各自的 RGB 偏移方向

7. 对比度与曲线特征
- 全局对比度（低 / 中 / 高，用数值 0–100）
- S 曲线强度（0–100）
- 是否使用 Film Curve（true/false）

8. 风格总结（仅一段话）
- 用一句话总结该照片的调色风格，但必须基于上面的数值结论。

最终输出：
- 仅输出 JSON
- 不要输出任何解释性文字


